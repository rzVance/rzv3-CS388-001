<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/detailLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FoodDetailActivity">

    <TextView
        android:id="@+id/food_amount_label"
        android:layout_width="155dp"
        android:layout_height="36dp"
        android:text="Calories"
        android:textSize="30sp"
        app:layout_constraintBottom_toBottomOf="@+id/enter_food_amount"
        app:layout_constraintEnd_toStartOf="@+id/enter_food_amount"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/enter_food_amount"
        app:layout_constraintVertical_bias="0.0" />

    <EditText
        android:id="@+id/enter_food_amount"
        android:layout_width="249dp"
        android:layout_height="64dp"
        android:layout_marginTop="180dp"
        android:ems="14"
        android:hint="Enter estimated carlories"
        android:inputType="textPersonName"
        android:minHeight="48dp"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.67"
        app:layout_constraintStart_toEndOf="@id/food_amount_label"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/food_notes_label"
        android:layout_width="156dp"
        android:layout_height="43dp"
        android:text="Name"
        android:textSize="30sp"
        app:layout_constraintBottom_toBottomOf="@+id/enter_food_notes"
        app:layout_constraintEnd_toEndOf="@id/food_amount_label"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/enter_food_notes"
        app:layout_constraintVertical_bias="0.333"
        app:layout_goneMarginLeft="16dp" />


    <EditText
        android:id="@+id/enter_food_notes"
        android:layout_width="247dp"
        android:layout_height="64dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp"
        android:ems="5"
        android:hint="Anything but McDonalds.."
        android:minHeight="48dp"
        android:textSize="18sp"
        app:layout_constraintStart_toStartOf="@+id/enter_food_amount"
        app:layout_constraintTop_toBottomOf="@+id/enter_food_amount" />

    <Button
        android:id="@+id/add_food_button_detail"
        android:layout_width="210dp"
        android:layout_height="76dp"
        android:layout_marginBottom="116dp"
        android:text="Add Food"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>